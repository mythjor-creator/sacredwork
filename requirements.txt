Django==6.0.3
python-dotenv==1.2.2

# Production server
gunicorn==23.0.0

# Production database (PostgreSQL)
# Note: psycopg2-binary may need a newer release for Python 3.14+.
# Alternative: install psycopg[binary] >= 3.0 once wheels are available.
# psycopg2-binary==2.9.10
dj-database-url==2.3.0

# Production static file serving (eliminates need for separate nginx for static)
whitenoise==6.8.2

# Image uploads for profile photos and galleries
Pillow==12.1.1
