Django==6.0.3
python-dotenv==1.2.2

# Production server
gunicorn==23.0.0

# Production database (PostgreSQL)
# psycopg (v3) — supports Python 3.12+ and works with Railway's managed Postgres
psycopg[binary]==3.2.6
dj-database-url==2.3.0

# Production static file serving (eliminates need for separate nginx for static)
whitenoise==6.8.2

# Image uploads for profile photos and galleries
Pillow==12.1.1

# Payments
stripe>=11.0.0,<12.0.0
